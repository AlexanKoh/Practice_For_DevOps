apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: devops-site
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: ghcr.io/alexankoh/nginx-custom:latest
          ports:
            - containerPort: 80

