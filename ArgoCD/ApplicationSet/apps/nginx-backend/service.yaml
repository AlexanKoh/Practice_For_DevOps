apiVersion: v1
kind: Service
metadata:
  name: nginx-backend
  labels:
    app: nginx-backend
spec:
  selector:
    app: nginx-backend
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
